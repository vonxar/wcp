package gomoku;

public class GameGomoku{
	  public static void main(String[] args){
	    Field field = new Field(8,8);
	    field.prepare();
//	    field.putKoma(2,2,"◯️");
	    field.feature();

	    System.out.println("---------------------------------------------------");

	  //メインループ
	  		while (true) {
	  			boolean playerCan = true;
//				boolean enemyCan = true;

	  		//プレイヤーの処理----------
//				field.print();
				System.out.println("あなたの番です");
				field.putKoma("⚫️");
				if (field.blackList.size() != 0) {
					Point set = null;
					while (true) {
						String temp = sc.next();
						char c0 = temp.charAt(0), c1 = temp.charAt(1);
						if ((c1<'a'&&'h'<c1)) {
							System.out.println("入力が正しくありません");
						}
						if  (temp.length()!=2 || (c0<'1'||'8'<c0) || (c1<'a'||'h'<c1)){
							System.out.println("入力が正しくありません");
						}
						else {
							int y = temp.charAt(0) - '0';
							int x = temp.charAt(1) - 'a' + 1;
							if (field.containSetPoints(y, x) == true) {
								set = new Point(y, x);
								field.revStone("⚫️", y, x);
								System.out.println("---------------------------------------------------");
								System.out.println("あなたは"+temp+"に石を置きました");
								break;
							}
							else {
								System.out.println("その場所には石を置くことができません");
							}
						}
					}
					field.setStone("⚫️", set);
				}
				else {
					System.out.println("あなたは石を置く場所がありませんでした");
					playerCanSet = false;
				}
	  }

	  }
	}